{
  "name": "@be-music/json",
  "version": "0.1.0",
  "description": "Internal Be-Music intermediate representation (not an exchange/distribution format)",
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build:bundle": "vite build",
    "build": "vite build && tsgo -b tsconfig.json",
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "typecheck": "tsgo -p tsconfig.typecheck.json --pretty",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check"
  },
  "dependencies": {
    "@be-music/utils": "workspace:*"
  }
}
