---
'@be-music/player-web-demo': patch
---

Track the player-web default-chrome refactor: update `index.html` / `styles.css` for the rearranged default gameplay layout, adjust `main.ts` wiring to follow the new chrome injection seam, and clean up `readtext-overlay.ts` against the same surface.
