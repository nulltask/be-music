{
  "name": "be-music",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "description": "Be-Music tooling monorepo",
  "scripts": {
    "build": "pnpm -r --sort --workspace-concurrency=Infinity run build:bundle && tsgo -b tsconfig.json",
    "clean": "pnpm -r run clean",
    "typecheck": "pnpm -r --parallel run typecheck",
    "lint": "pnpm -r --parallel run lint",
    "lint:fix": "pnpm -r --parallel run lint:fix",
    "format": "pnpm -r --parallel run format",
    "format:check": "pnpm -r --parallel run format:check",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "bench:parser": "pnpm --filter @be-music/parser run bench",
    "bench:parser:check": "pnpm --filter @be-music/parser run bench:check",
    "bench:parser:update-baseline": "pnpm --filter @be-music/parser run bench:update-baseline",
    "player": "pnpm --filter @be-music/player run dev",
    "player:sea": "pnpm --filter @be-music/player run build:sea",
    "editor": "pnpm --filter @be-music/editor run dev",
    "audio-render": "pnpm --filter @be-music/audio-renderer run dev",
    "parse": "pnpm --filter @be-music/parser run dev",
    "stringify": "pnpm --filter @be-music/stringifier run dev"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@typescript/native-preview": "7.0.0-dev.20260302.1",
    "@vitest/coverage-v8": "^4.0.18",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.3",
    "vite": "^8.0.0-beta.16",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
